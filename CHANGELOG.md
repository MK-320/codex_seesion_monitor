# Changelog

## V1.1.0：完整 Tool Trace

- 新增 Codex 工具调用、结果、搜索、未知事件和解析状态的完整时间线，保留全局序号与并行批次视图。
- 支持全文/仅元数据检索、严格时序切换、原始事件查看、分页加载、复制和本地导出。
- 自定义工具调用与结果可以正确配对；新增工具发现视图，并在日志轮转或同大小替换时保持事件完整。
- 初始历史回填在后台进行，导出采用流式读取，保留期按事件时间执行，重启后首屏不会显示不完整快照。
- Trace 默认仅保存于本机用户数据目录，支持保留时长、磁盘占用查看和一键清除（不影响 Codex 原始会话）。
- 提供公开发布前的自动敏感信息与历史扫描门禁，帮助维护者避免误发布本机数据。

## V6.3.9：会话活跃度与误报治理

- 将会话状态拆分为执行状态、活跃度和关注状态，普通静默不再直接显示为“卡住”。
- 在工具执行时间较长、长时间没有新进展或监控数据过期时，使用明确且可解释的状态提示。
- 新增会话提醒阈值设置，提醒按事件基线去重；切换项目、会话或断开连接时不会误报旧状态。
- 设置页按“会话”和“日志设置”组织相关选项，日志隐私说明与日志设置放在同一处。
- 窄窗口继续保持无横向溢出的紧凑布局；项目和会话右键菜单保留常用复制操作，并屏蔽 WebView 调试入口。
- 浏览器端和桌面端统一处理数据过期状态，避免在无法确认实时数据时给出误导性提醒。

## V6.3.8：桌面设置与本地日志

- 新增桌面设置页，按概览、日志、日志设置和隐私说明分区展示应用信息。
- 手动检查更新入口移至“设置 > 关于”，顶部仅保留高频监控操作。
- 日志查看器支持最近 24 小时查询、级别/组件/事件码/摘要筛选、分页加载（最多 1000 条）和紧凑行展开。
- 展开记录显示磁盘中实际保存的完整脱敏 JSONL，统一使用 V6.3.8 snake_case 日志 schema，并标记 `tauri` 或 `sidecar` 来源。
- 日志级别统一显示为 `DEBUG`、`WARN`、`ERROR`，并覆盖 sidecar、窗口、通知、更新和诊断操作等排障事件。
- Python sidecar 通过版本化 stdout 事件接入统一日志；Tauri 负责二次脱敏、滚动存储、旧日志清理和容量上限控制。
- 新增全部保留日志的本地 ZIP 导出、导出预览、进度显示、取消和导出前再次脱敏。
- 导出对话框默认定位到用户可写的 Downloads 目录，避免安装目录权限导致导出失败。
- 会话详情顶部的元数据与状态事实改为默认收起的可折叠面板，释放时间线可视空间，需要时可展开查看完整信息。
- 修复桌面开发启动误继承受限 `target-codex` 缓存导致 Cargo 扫描失败的问题。
- `npm run desktop:dev` 现在强制使用仓库根目录下被 Git 忽略的 Rust target 目录，直接启动也不会再读取受限测试缓存。
- 浏览器开发命令现在会自动启动并回收本地监控服务，避免 Vite 代理请求直接报 `ECONNREFUSED`。
- 日志列表在滚动或展开记录时保留当前位置，并以“有新日志”提示代替强制刷新；窄窗口继续采用相对单位和 intrinsic sizing，不使用媒体查询。
- 收紧会话队列卡片的状态列宽和列间距，减少宽窗口下无意义的横向留白，保持会话标识、项目和动作信息的紧凑对齐。

## V6.3.7：通知直达与本地诊断日志

- 当一条桌面提醒只对应一个有效的新关注事件时，点击提醒会直接打开对应会话，并定位到相关任务或失败工具。
- 提醒包含多个事项、目标已经失效或实时连接不可用时，会安全回到当前“未处理”收件箱，不会猜测或打开错误会话。
- 新增仅保存在本机的诊断日志：默认记录警告和错误，排障时可以临时开启 30 分钟调试日志，并在到期后自动恢复。
- 诊断界面可以查看近期脱敏摘要、打开日志目录、复制脱敏信息和清空日志；日志按 5 MB 滚动，总容量默认 50 MB 且可调整。
- 诊断记录不会包含会话正文、命令、完整项目路径、凭据、事件标识或会话标识，也不会自动上传。

## V6.3.6：通知回到注意力收件箱

- 点击新的关注提醒后，仍在运行的桌面应用会恢复到前台并直接打开实时“未处理”收件箱；浏览器通知点击也会进入同一收件箱。
- 收件箱会清除项目、搜索和时间限制，按需关注优先排序；桌面会打开会话列并选中当前第一项，移动端则保留在会话队列中。
- 当当前会话因处理、项目、搜索或时间筛选离开可见列表时，详情会自动切换到下一项；列表为空时会清空旧详情，不会继续显示过期内容。
- 提醒仍只显示聚合数量，不包含会话标识、项目路径、消息、工具输入或恢复信息；不会自动标记或撤销任何关注事件。

## V6.3.5：关注事件历史定位

- 打开较早的关注事件时，详情会自动加载目标附近的有限历史，并定位对应任务或失败工具。
- 定位成功后会显示“历史定位视图”，可随时使用“回到最近”恢复最新详情。
- 历史锚点已失效时会安全回退到最近详情；会话本身已失效时仍显示会话已移除，不会误回退。
- 网络或详情加载失败时保留明确的重试入口。
- 本次变化不修改关注处理状态、Codex 会话文件或既有持久化，也不新增 API 端点或依赖。

## V6.3.4：最近一次批量处理撤销

- 批量标记关注事件后，显示“撤销最近一次批量处理 (N)”。
- 一次恢复整个最近批次，新批次会替换旧批次，后来产生的新关注事件不会被误撤销。
- 如果单独处理或撤销最近批次中的事项，该批次的撤销入口会立即失效，避免覆盖后续操作。
- 撤销快照仅保存在当前页面内，刷新或关闭页面后无法继续撤销，也不提供重做、倒计时或跨窗口同步。

## V6.3.3：桌面开发启动可靠性

- 修复 Tauri updater 配置为空导致桌面端启动崩溃的问题。
- 本地开发无需生产更新密钥即可启动，正式版的更新、验签和 CI 配置保持不变。
- 这主要是稳定性修复，不是新增用户功能。

## V6.3.2：注意力处理闭环

- 将工具失败、任务中止和卡住状态整理为可处理的关注事项，新事件会自动重新进入未处理状态。
- 新增未处理/已处理筛选、单项处理与撤销、当前筛选批量处理，以及对应详情定位和安全复制。
- 批量处理只作用于点击瞬间符合当前项目、筛选、搜索和时间范围的可见未处理事项，单次最多处理 1000 条。
- 新关注事件会使用新的事件标识重新进入未处理状态，已经处理的旧事件不会阻止后续提醒。
- 详情可以定位对应任务和工具调用；会话 ID 可以直接复制，只有格式安全的 ID 才提供 `codex resume` 命令。
- 处理记录只保存在本机，最多保留 30 天和 1000 条；浏览器存储不可用时，当前页面仍可处理和撤销，但刷新后状态不会保留。
- 标记处理只改变本机收件箱视图，不修改 Codex 会话文件或服务端状态。
- 详情只在当前已加载的时间线中定位目标，找不到时会明确提示，不会自动读取更早历史。

## V6.3.0：Windows 正式发布准备

- 完成 Windows 当前用户安装包开发，内置 WebView2 引导程序，卸载时会明确提示保留用户配置。
- 增加应用启动后自动检查和手动检查更新，并显示下载进度、失败重试和同一主版本限制。
- 连续启动失败时会打开恢复指引；没有上一稳定版本时也会明确说明无法自动恢复。
- 补齐代码签名、版本校验、更新清单和发布门禁。
- 本版开发和本地验证已完成，但正式签名、生产更新密钥和 Windows 10/11 验收仍需外部配置，因此尚未公开发布。

## V6.2.1：原生桌面通知

- 桌面端已接入 Windows 原生通知，浏览器模式继续使用浏览器通知。
- 通知权限被拒绝后会显示明确的不可用状态。
- 功能开发已完成，但 Windows 中最终可见的通知体验仍需通过具有应用身份的正式安装包验证。

## V6.2.0：桌面生命周期与原生目录选择

- 增加单实例、系统托盘、关闭到托盘和显式退出，退出时会清理后台进程与端口。
- 桌面版项目导入改用原生目录选择器。
- 修复开发态桌面导入项目时出现的 `Failed to fetch`。
- 桌面后端不再依赖 Tk 目录选择器，随应用打包的后端也不再包含 Tk 组件。

## V6.1.0：自包含认证后端

- 桌面应用自带 Python 后端，目标电脑无需预装 Python 或 uv。
- 后端由桌面应用安全启动和回收，并使用随机本地端口与启动令牌保护连接。

## V6.0.0：Windows 桌面应用原型

- 新增基于 Tauri 2 的 Windows 桌面壳，复用现有 React 页面和 Python 服务。
- 现有会话总览、项目切换、详情时间线和实时更新可以直接在原生桌面窗口中使用。
- 增加桌面开发启动与进程清理，关闭后会释放前后端端口。

## V5.3.1：通知基线修复

- 修复应用启动时把已有错误或卡住会话误报为新通知的问题。
- 首次同步完成后出现的新关注事件仍会正常提醒。

## V5.3.0：长时间运行稳定性

- 增强事件突发、服务停止和同端口重启后的恢复能力。
- 服务中断时界面会提示数据可能过期，同一端口重启后可自动恢复实时连接。
- 通知设置可跨服务重启保留，无需重新配置项目或通知开关。
- 项目管理、长时间线和移动布局继续正常工作。

## V5.2.0：浏览器安全基线

- 页面和接口增加内容安全策略、防嗅探、防嵌入和来源限制，降低本地会话内容被第三方页面读取或执行的风险。
- 页面与接口不缓存，带哈希的静态资源使用长期缓存，更新后仍会加载正确版本。
- 浏览器实时连接会拒绝跨来源访问，同时保留本地客户端兼容性。

## V5.1.1：工作区布局重设计

- 桌面项目列和会话列折叠后保留等高窄轨道，并移除无效的返回按钮。
- 移动端改为项目、会话和详情分步导航，避免内容拥挤、重叠和横向溢出。

## V5.1.0：本地注意力通知

- 新增默认关闭的浏览器通知，只有用户主动启用后才会请求权限。
- 初始数据不会触发提醒，仅页面隐藏后新出现的关注会话会发送通知。
- 同一会话在冷却时间内不会重复提醒，多条关注事项会合并为不含敏感内容的数量提示。
- 本地只保存通知开关，不保存会话标识、路径或正文。

## V5.0.0：增量监控管线

- 文件变化改为增量处理，并自动合并同一路径的重复事件。
- 低频完整校准可以发现新增、移除和漏报，使长时间运行能够自动恢复。
- 新增或移除项目时会立即重建监控范围，避免继续跟踪失效目录。
- 诊断界面会显示已知会话文件数、最近完整校准时间、校准次数和已合并的重复事件数。

## V4.6.2：项目状态可见

- 项目导航会明确显示“已保存”“仅本次”和“目录失效”，失效目录同时使用文字与颜色提示。

## V4.6.1：安全移除项目

- 在项目导航中补齐可见的项目移除入口和确认提示，只会停止监控，不会删除磁盘文件。
- 前后端共同阻止移除最后一个项目，并加强项目管理操作的来源限制。

## V4.6.0：脱敏诊断

- 新增诊断预览和确认下载，便于排查本地监控问题。
- 诊断报告不包含会话标识、项目路径、正文、工具内容或环境变量值。
- 诊断导出失败时只显示错误，不会中断正在运行的监控。

## V4.5.1：响应式长会话工作区

- 项目列和会话列可独立折叠，长时间线支持回到顶部和返回最近内容。
- 限制页面同时保留的详情数量，并改善手机到宽屏的连续布局。

## V4.5.0：自动化发布验收

- 统一发布检查和浏览器验收流程，提高版本交付可靠性；本版没有新增用户功能。

## V4.4.0：长会话时间线

- 详情默认展示最近内容，并支持继续加载更早历史、仅看错误和跳到最新。
- 补充加载失败、会话移除和空时间线等明确状态。
- 长文本和工具详情可以折叠，复制操作会显示成功或失败反馈。
- 切换会话时会取消旧请求，避免旧详情覆盖当前选择。

## V4.3.0：项目持久化与生命周期

- 显式导入的项目可以保存在本机，并在应用重启后恢复。
- 建立项目安全移出能力、缺失目录提示和嵌套项目会话重新归属。
- 移除监控项目不会删除磁盘目录。
- 损坏的本机配置会安全降级为空配置，缺失目录不会参与扫描。
- 启动时可以选择忽略已保存项目，仅监控本次指定的目录。

## V4.2.0：健康状态与断线恢复

- 连接区可以区分连接中、在线、重连和数据可能过期，并显示最后同步时间。
- 服务短暂离线时会保留已有会话，恢复后无需刷新页面。

## V4.1.0：会话定位

- 支持按会话、项目、路径、当前动作和用户摘要搜索。
- 新增状态筛选、关注筛选、时间范围、排序和一键清除。
- 定位条件可随页面刷新恢复，且不会把完整路径或正文写入地址栏。
- 当前会话离开筛选结果时会自动选择下一项或显示空状态，不残留错误详情。

## V4.0.0：可靠事件解析

- 同时兼容旧式与现代 Codex 事件，并识别工具调用、工具结果、任务中止和异常状态。
- 重复事件会自动去重，未知或损坏记录会形成诊断而不是破坏页面。
- 前端不再依赖动作文字猜测错误状态。

## V3.3.1：发布一致性

- 清理误跟踪的开发工具文件，并加强版本和静态资源一致性检查；本版没有新增用户功能。

## V3.3.0：动态导入发布收口

- 统一动态导入相关版本和使用说明，重复导入不会产生重复项目。
- 当时导入的项目仅在当前服务进程中保留，重启后不会自动恢复。

## V3.2.0：图形化导入与一键启动

- 项目导航新增本机目录选择和导入入口，取消选择不会改变现有项目。
- Windows 新增双击启动器，可检查端口、启动服务并自动打开页面。

## V3.1.0：运行时项目导入

- 服务运行期间可以导入已有本地项目，历史会话会立即出现，无需刷新页面。
- 无效目录会被拒绝，重复导入不会产生重复项目，嵌套项目会正确归属。

## V3.0.0：动态导入规划

- 确定图形化项目导入、历史会话扫描和 Windows 一键启动路线；本版主要完成产品规划。

## V2.3.0：多项目发布验收

- 补齐多项目启动和切换说明，明确项目切换只过滤现有实时数据，不会重启服务或新建实时连接。
- 本阶段的项目列表仍来自启动参数，不支持在网页中新增、删除、编辑或保存项目。

## V2.2.0：多项目工作区

- 主界面升级为项目导航、会话队列和详情时间线三栏工作区。
- 聚合视图会显示会话所属项目，并支持即时切换到单个项目。
- 项目导航提供“全部项目”和单项目入口，并显示各项目的会话与关注数量。
- 切换项目只过滤同一份实时数据，无需刷新页面或重新连接。
- 启动脚本支持传入多个项目，桌面和窄屏导航均保持可用。

## V2.1.0：多项目监控

- `--project` 可以重复传入以监控多个项目；未传入时仍使用当前工作目录。
- 嵌套项目会按最具体的目录归属，不属于任何已配置项目的会话不会被读取或展示。
- 不同项目中的同名会话互相隔离，会话列表、详情和实时更新都会显示项目归属。
- 本阶段的项目清单只来自启动参数，不能在网页中增删或保存。

## V2.0.0：多项目路线规划

- 确定同一页面查看和切换多个本地项目的产品路线；本版主要完成产品规划。
- 规划包含重复项目参数、最具体目录归属、跨项目同名会话隔离、单一实时连接和三栏工作区。
- 项目切换应即时完成，不重启服务、不重新加载页面，也不为每个项目创建单独连接。
- 该路线继续保持本地只读，不包含远程访问、账号系统、通知或跨浏览器同步。

## V1.3.0：本地启动体验

- 新增 PowerShell 启动脚本，可以直接启动本地监控页面。
- 脚本复用项目内 uv，不会在每次启动时自动同步依赖，并保持仅监听本机。
- 脚本默认监控当前目录，也支持指定项目目录、会话目录、端口和卡住阈值。
- 启动脚本不会修改 Codex 会话文件、开放公网访问或自动下载依赖。

## V1.2.0：详情核对信息

- 详情页新增工作路径、会话来源、CLI 版本和模型提供方。
- 工具调用会显示名称、状态、输入与结果摘要、耗时和执行中状态。
- 长路径可以自动换行，工具详情在窄屏下保持可读且不会产生横向溢出。
- 日志内容只作为文本展示，不会作为网页代码执行。

## V1.1.0-dev：实时会话状态

- 运行中会话的当前任务时长会持续更新，长时间无新事件时会标记为“疑似卡住”。
- 后续活动可以恢复为运行中，无需刷新页面。
- 新增 `--stuck-seconds` 参数调整卡住阈值，默认 120 秒且只接受正整数。
- 状态变化继续通过实时连接推送，前端不会轮询会话列表。

## V1.0.1：交互规则修复

- 桌面端隐藏没有实际作用的“返回总览”按钮。
- 移动端详情仍保留返回会话队列的入口。

## V1.0.0：首个可用版本

- 发布本地只读的单项目 Codex 会话监控页面。
- 只读取属于指定项目目录的历史和新增会话，项目外会话不会被解析或展示。
- 通过增量读取 Codex 日志和实时连接推送更新，新会话、任务状态和工具结果无需手动刷新即可出现。
- 会话总览显示来源、运行状态、当前任务、用户消息摘要、当前动作、任务数量和近似上下文大小。
- 详情按任务展示用户消息、工具调用顺序、输入与结果摘要以及 Agent 文本。
- 支持状态筛选、需关注排序、主题切换、桌面布局和移动端队列/详情切换。
- 上下文大小只按字符数近似展示，不代表精确 token 数量。
- 监控器完全只读，只监听本机，不修改 Codex 会话文件；本版不包含跨项目、远程访问、账号或通知功能。
